import React from 'react';

import css from './Footer.module.css';

const Footer = () => {
    return (
        <footer id="footer" className={css.footer}>
            <h6>React SSR + TypeScript</h6>
        </footer>
    );
};

export default Footer;
